import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'chan.ecom.delivery',
  appName: 'Grocery',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
